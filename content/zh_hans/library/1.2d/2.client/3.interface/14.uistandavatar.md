---
title: UIStandAvatar 立绘组件
description: 用于指定一个范围min和max，在min和max之间滑动取得一个值的组件
---

> 主要维护人员：[Karson.DS](https://www.gamecreator.com.cn/user/1)<br>
> 创建时间：2020-01-27

## 关联性

- `继承` → [UIAvatar](./uiavatar) → [UIBase](./uibase) → [GameSprite](./gamesprite) → [Sprite](./sprite) → [TreeNode](./treenode) → [EventDispatcher](./eventdispatcher)
- `子类` 无

## 说明

封装了立绘的界面组件

### 相关事件

```ts
// 加载完成时候事件
EventObject.LOADED 
// 动作播放完毕时
Avatar.ACTION_PLAY_COMPLETED
// 当确实到达了新的一帧后派发，本体如果没有实际的帧则不派发
Avatar.RENDER 
```

### 使用方法

```ts
var a = new UIStandAvatar();
a.avatarID =5;
stage.addChild(a);
// 事件监听示例
a.on(EventObject.LOADED,this,this.onLoaded);
a.on(Avatar.ACTION_PLAY_COMPLETED,this,this.onActionPlayComplete);
a.on(Avatar.RENDER,this,this.onRender);
```

## Public 属性

|                                     |
| ----------------------------------- |
| **flip** : boolean;<br>是否水平翻转 |
